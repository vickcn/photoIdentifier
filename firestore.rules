rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 本專案第一階段由 FastAPI / Cloud Run 後端透過服務帳號讀寫 Firestore。
      // 瀏覽器端不直接連 Firestore，因此 client SDK 預設不開放。
      allow read, write: if false;
    }
  }
}
